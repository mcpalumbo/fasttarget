# Configuration file

# ---- Organism information ----
organism:
  # name: Complete with a short alias/name for your genome, without spaces. Example: PAO.
  name: name
  # tax_id: Fill with the number of NCBI Taxonomy ID of the specie of your organism. Example: 287 for Pseudomonas aeruginosa.
  # It is used to obtain the genomes to calculate the core genome.
  tax_id: tax_id
  # gbk_file: Complete with the full path of the GBK file of your genome.
  gbk_file: /path/to/mypathogen.gbk

# ---- CPU usage preferences ----
# cpus: Complete with the number of CPUs used to run this pipeline.
cpus: None

# ---- Metabolism ----
metabolism:
  enabled: False
  # sbml_file: Fill with the path of the SBML file generated by Pathway Tools.
  sbml_file: /path/to/model.sbml
  # chokepoint_file: Fill with the path of the chokepoints file report generated by Pathway Tools.
  chokepoint_file: /path/to/chokepoints.txt
  # smarttable_file: Fill with the path of the Genes smarttable generated by Pathway Tools.
  smarttable_file: /path/to/genes_smarttable.tsv

# ---- Structures ----
structures: 
  enabled: False
  # proteome_uniprot: Complete with the proteome ID of Uniprot for your organism. Example: UP000002438 for Pseudomonas aeruginosa PAO1.
  proteome_uniprot: proteome_uniprot

# ---- Core ----
core:
  enabled: False
  roary: False
  corecruncher: False
  # corecruncher_script: Fill with the path of the corecruncher script. Example: /home/user/CoreCruncher-master/corecruncher_master.py.
  corecruncher_script: /path/to/corecruncher_master.py

# ---- Offtarget ----
offtarget:
  enabled: False
  human: False
  microbiome: False
  # microbiome_identity_filter: Filter value of %identity, only hits with higher values will be used.
  microbiome_identity_filter: 0
  # microbiome_coverage_filter: Filter value of query coverage, only hits with higher values will be used.
  microbiome_coverage_filter: 0

# ---- DEG ----
deg:
  enabled: False
  # deg_identity_filter: Filter value of %identity, only hits with higher values will be used.
  deg_identity_filter: 0
  # deg_coverage_filter: Filter value of query coverage, only hits with higher values will be used.
  deg_coverage_filter: 0

# ---- Localization ----
psortb:
  enabled: False
  # gram_type: Complete with the type of organism: 'n' (Gram-), 'p' (Gram+), or 'a' (archaea).
  gram_type: n

# ---- Metadata ----
metadata:
  enabled: False
  # meta_tables: Fill with the full paths of the metadata tables.
  meta_tables:
    - /path/to/meta_table1.tsv
    - /path/to/meta_table2.tsv
    # Add more paths as needed
